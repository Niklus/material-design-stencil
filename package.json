{
  "name": "material-design-stencil",
  "private": true,
  "version": "0.0.1",
  "description": "Material Design Stencil Starter App",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "stencil build && gulp && workbox-cli generate:sw",
    "dev": "sd concurrent \"stencil build --dev --watch\" \"stencil-dev-server\" ",
    "serve": "stencil-dev-server",
    "start": "npm run dev",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "@stencil/core": "next",
    "@stencil/router": "0.0.4",
    "material-components-web": "^0.19.0"
  },
  "devDependencies": {
    "@stencil/dev-server": "latest",
    "@stencil/utils": "latest",
    "gulp": "^3.9.1"
  }
}
